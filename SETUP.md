# מדריך הקמה — הספרייה המשפחתית 📚

המדריך הזה מפעיל את המערכת בפעם הראשונה. זה נעשה **פעם אחת בלבד**, ולוקח בערך 15 דקות.
אין צורך בידע טכני — רק להעתיק ולהדביק.

---

## שלב 1 — יצירת בסיס הנתונים (Supabase)

1. היכנסי ל‑**supabase.com** ולחצי **Start your project**.
2. התחברי עם **Google** (הכי מהיר).
3. לחצי **New project**. מלאי:
   - **Name**: `family-library`
   - **Database Password**: לחצי "Generate a password" ו**שמרי אותו** במקום בטוח.
   - **Region**: בחרי `Europe (Frankfurt)` או הקרוב אלייך.
4. לחצי **Create new project** והמתיני ~2 דקות שהפרויקט יוקם.

---

## שלב 2 — הרצת מבנה בסיס הנתונים

1. בתפריט הצד ב‑Supabase, לחצי על **SQL Editor** (אייקון של `</>`).
2. לחצי **New query**.
3. פתחי את הקובץ `supabase/all_in_one.sql` מתוך תיקיית הפרויקט, **העתיקי את כל התוכן**, והדביקי לחלון.
4. לחצי **Run** (או Ctrl+Enter).
5. אמור להופיע "Success". זהו — כל הטבלאות וההרשאות נוצרו.

---

## שלב 3 — העתקת שני המפתחות

1. בתפריט הצד לחצי **Project Settings** (גלגל שיניים) → **API**.
2. תמצאי שם:
   - **Project URL** — משהו כמו `https://abcdefgh.supabase.co`
   - **anon public** — מפתח ארוך שמתחיל ב‑`eyJ...`
   - **service_role** — מפתח ארוך נוסף (סודי!)
3. בתיקיית הפרויקט, צרי קובץ בשם `.env.local` (בדיוק כך, עם הנקודה בהתחלה) עם התוכן:

```
NEXT_PUBLIC_SUPABASE_URL=כאן_ה-Project_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY=כאן_מפתח_anon
SUPABASE_SERVICE_ROLE_KEY=כאן_מפתח_service_role
```

> 💡 יש תבנית מוכנה בקובץ `.env.local.example` — אפשר להעתיק ולשנות שם ל‑`.env.local`.

---

## שלב 4 — כיבוי אישור אימייל (מומלץ למשפחה)

כדי שבני המשפחה יוכלו להתחבר מיד בלי לאשר אימייל:

1. ב‑Supabase: **Authentication** → **Sign In / Providers** → **Email**.
2. כבי את האפשרות **Confirm email**.
3. שמרי.

---

## שלב 5 — הפעלת המערכת במחשב

בתיקיית הפרויקט, פתחי טרמינל והריצי:

```bash
npm run dev
```

פתחי בדפדפן: **http://localhost:3000**

---

## שלב 6 — יצירת המשתמשים והגדרת המנהלת

1. במסך שנפתח, לחצי **חשבון חדש** וצרי חשבון לעצמך (אמא) — שם, אימייל וסיסמה.
2. עכשיו נהפוך אותך ל**מנהלת**: חזרי ל‑Supabase → **SQL Editor** → New query, הדביקי (עם האימייל שלך):

```sql
update profiles set role = 'admin'
where id = (select id from auth.users where email = 'האימייל-שלך@gmail.com');
```

לחצי **Run**. התנתקי והתחברי שוב באפליקציה — עכשיו יש לך לוח בקרה מלא.

3. כל אחד משאר בני המשפחה (דוד, הלליה, אביגיל, אריאל, אליענה) ייצור לעצמו חשבון דרך "חשבון חדש".

---

## מה עכשיו?

- **הוספת ספרים**: כמנהלת, לחצי "הוספת ספר" — אפשר לחפש לפי ISBN למילוי אוטומטי.
- כל בן משפחה יכול לסמן "קראתי", לדרג, להוסיף תגיות, ולסמן ספר "טעון עיון" (רק את רואה את הדיווחים).

## העלאה לאינטרנט (בהמשך)

כשנרצה שהמשפחה תיכנס מכל מקום (לא רק מהמחשב הזה), נעלה את האפליקציה ל‑**Vercel** בחינם ונחבר לאותו Supabase. זה שלב נפרד — נעשה אותו יחד כשתהיי מוכנה.
